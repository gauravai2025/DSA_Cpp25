#include <bits/stdc++.h>
using namespace std;

int main() {
    cout<<"enter string\n";
    string str;
    cin>>str;
    reverse(str.begin(),str.end());
    cout<<"reversed string: "<<str;
    return 0;
}
